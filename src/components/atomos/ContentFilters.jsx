import styled from "styled-components";

const ContentFilters = styled.span`
  position: relative;
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
`;

export default ContentFilters;
